let sum = ()=>{
    console.log("I am sum")
}

let s=sum ()
console.log(s)

//By default, a function will return with undefined value.
//Here, In the above program, the output will be "I am sum". But after the function, we have not called 's', 
//so the value will give undefined by default.