let ar1=[2,3,1]
console.log(ar1[0])

//in array, order of element matters. 

//but in the case of object, the order of element doesn't matter.
let info ={
    
    age:22,
    isMarried:false,
    name: "Anjali",
    address:"ABC"
}

console.log(info.name)