//make an arrow function that takes a input and return its uppercase version

export let uppercase = (input)=> {
    let upper = input.toUpperCase()
    return upper
}

//make an arrow function that takes an input and return its lowercase version

export let lowercase = (value)=>{
    let lower = value.toLowerCase ()
    return lower
}