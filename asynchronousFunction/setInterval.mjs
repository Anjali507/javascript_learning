//The setInterval function is used to repeatedly execute a given function or block of code at specified intervals. 
//It allows to create a recurring timer that triggers a function repeatedly after a specified time interval in milliseconds.

setInterval(() => {
    console.log("I am setInterval")
}, 3000);

